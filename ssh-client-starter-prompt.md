# SSH 클라이언트 데스크톱 앱 - 개발 시작 프롬프트

> 이 문서는 Claude Code 등 AI 코딩 에이전트에게 그대로 붙여넣어 프로젝트를 시작할 때 사용하는 프롬프트입니다.

---

## 프로젝트 개요

MobaXTerm, Termius를 참고한 SSH 클라이언트 데스크톱 애플리케이션을 만들어줘.
편안한 SSH 연결 경험, 다중 세션 관리, 세션 연결 시 자연스럽게 전환 가능한 SFTP 파일 탐색기를 핵심으로 한다.

## 기술 스택

- **런타임/프레임워크**: Electron + TypeScript
- **프론트엔드**: React + Vite
- **터미널 렌더링**: xterm.js (+ xterm-addon-fit, xterm-addon-search, xterm-addon-webgl)
- **SSH/SFTP**: ssh2 (Node.js 네이티브 SSH2 구현, SFTP 서브시스템 포함)
- **로컬 PTY(필요 시, 로컬 쉘 지원용)**: node-pty
- **상태 관리**: Zustand 또는 Redux Toolkit
- **세션/자격증명 저장**: electron-store + OS 키체인 연동(keytar 또는 safeStorage API)로 비밀번호/키 암호화 저장
- **빌드/배포**: electron-builder (Windows/macOS/Linux 크로스 빌드)

> 선정 이유: Node.js/TypeScript 생태계가 SSH2·SFTP·PTY 라이브러리 성숙도가 높고, 웹 프론트엔드 경험을 그대로 활용할 수 있어 개발 속도가 빠름. Tauri(Rust)도 대안이 될 수 있으나 SSH/SFTP 관련 성숙한 크레이트가 상대적으로 적어 초기 개발 리스크가 큼.

## 핵심 기능 요구사항

### 1. 세션 관리
- 세션/서버 정보를 그룹(폴더) 단위로 저장 (호스트, 포트, 유저, 인증방식, 색상/태그)
- 인증 방식: 비밀번호, SSH 키(passphrase 포함), SSH 에이전트 연동
- 저장된 세션 목록에서 더블클릭/우클릭으로 빠른 연결
- 최근 연결 기록, 즐겨찾기

### 2. 멀티 세션 UI
- 탭 기반 다중 세션 (탭별 독립 터미널 인스턴스)
- 화면 분할(가로/세로) 지원 - 여러 세션 동시 보기
- 세션 간 명령어 동시 입력(브로드캐스트) 옵션 (MobaXTerm의 "Multi-exec" 유사 기능)

### 3. SFTP 통합
- 세션 연결 시 터미널과 SFTP 탐색기를 나란히 또는 탭 전환으로 즉시 사용 가능해야 함 (같은 SSH 커넥션을 공유해서 별도 재인증 없이 SFTP 서브시스템 오픈)
- 드래그앤드롭으로 로컬 ↔ 원격 파일 업/다운로드
- 파일 목록 새로고침, 이름변경/삭제/권한변경(chmod), 새 폴더 생성
- 업/다운로드 진행률 표시, 다중 파일 전송 큐

### 4. 포트포워딩/터널링
- 로컬/원격/다이나믹(SOCKS) 포트포워딩 설정 UI
- 세션별로 여러 개의 포워딩 규칙 저장 및 재사용

### 5. 편의 기능
- 터미널 테마(색상 스킴) 커스터마이징
- 매크로/스니펫 (자주 쓰는 명령어 저장 후 클릭 실행)
- 세션별 로그 기록(터미널 출력 파일로 저장)
- 클립보드 동기화, 검색(xterm search addon)

## 성능 주의사항 (터미널 입출력)

빠른 로그 스트리밍, 대용량 `cat` 출력 등 초당 대량의 데이터가 쏟아지는 상황에서도 끊김이 없어야 한다. 다음을 반드시 반영해줘:

1. **렌더러**: xterm.js 기본 DOM 렌더러 대신 `xterm-addon-webgl`을 사용해서 GPU 렌더링으로 처리 (DOM 렌더러는 대량 출력 시 리플로우 비용으로 버벅임 발생)
2. **IPC 배칭**: ssh2 커넥션의 `data` 이벤트를 받을 때마다 `ipcRenderer`/`webContents.send`를 즉시 호출하지 말 것. Electron IPC는 호출마다 구조적 복제 오버헤드가 있으므로, main process에서 짧은 간격(8~16ms, 한 프레임 주기)으로 데이터를 버퍼에 모았다가 flush하는 배칭 큐를 구현해줘. ssh2 stream을 renderer로 그대로 pipe하지 않는다.
3. **세션 간 격리**: 세션마다 독립적인 IPC 버퍼/flush 로직을 두어, 한 세션의 출력 폭주가 다른 세션의 입력 반응성에 영향을 주지 않도록 한다. `data` 콜백 안에서 로그 파일 동기 기록, 정규식 파싱 등 무거운 동기 작업을 피한다.
4. **비활성 탭/분할화면 최적화**: 포커스가 없는 세션은 실제 paint를 하지 않고 write 큐만 쌓아두는 방식으로 처리해서, 여러 xterm 인스턴스를 동시에 렌더링할 때 GPU 메모리 소모를 줄인다.
5. **스크롤백 제한**: xterm.js `scrollback` 옵션에 상한(예: 5000~10000줄)을 두어 메모리 증가를 막는다. 전체 로그가 필요하면 터미널 버퍼와는 별도로 파일에 기록한다.

## 아키텍처 가이드

- Electron의 **Main process**에서 ssh2 커넥션과 PTY를 관리하고, **Renderer process**는 IPC(contextBridge/ipcRenderer)를 통해 터미널 입출력 스트림만 주고받도록 설계 (보안 - nodeIntegration은 false, contextIsolation true 유지)
- SSH 연결과 SFTP는 동일 Connection 객체에서 서브시스템만 분리 (`conn.sftp()`) 하여 재인증 없이 전환
- 세션 상태(연결/끊김/재연결)는 중앙 상태 스토어에서 관리하고 UI가 구독하는 구조로 설계
- 자격증명은 절대 평문 저장 금지 - OS 레벨 보안 저장소 사용

## 요청 사항

1. 위 스택으로 프로젝트 스캐폴딩부터 시작해줘 (Electron + Vite + React + TypeScript 템플릿 구성)
2. MVP 범위: 세션 저장/연결 → 터미널 탭 → SFTP 탐색기(업/다운로드) 순으로 단계별 구현
3. 각 단계마다 실행 가능한 상태로 커밋 단위를 나눠줘
4. 보안 관련 부분(자격증명 저장, IPC 격리)은 특히 신경 써서 구현해줘

---

*작성 시 아직 정하지 않은 부분 (필요 시 에이전트에게 추가로 지정): 지원 OS 우선순위, 라이선스/배포 방식(오픈소스 여부), UI 디자인 톤(다크모드 기본 여부 등)*
